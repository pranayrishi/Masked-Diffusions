# Local env / IDE
.DS_Store
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/
.python-version

# Virtual environments
venv/
.venv/
env/

# Datasets and large artifacts (regenerated, not source)
data/
*.npy
*.npz
*.parquet
*.csv
!tests/**/*.csv

# Model checkpoints and training outputs
checkpoints/
runs/
wandb/
outputs/
logs/
*.pt
*.pth
*.bin
*.safetensors
metrics.jsonl
metrics.csv

# Cluster / credentials (must never be committed)
# Note: .gitignore does NOT support inline comments; each pattern must be on its own line.
cluster_config.yaml
cluster_config.local.yaml
slurm/cluster_config.local.yaml
# Rendered Slurm scripts contain NetID and absolute paths — generated by
# slurm/_render_scripts.sh into the directory below.
slurm/_rendered/
.env
.env.local
kaggle.json
*.kaggle.json
slurm-*.out
slurm-*.err

# Local smoke run outputs
medium_smoke_runs/
baseline/runs/
entropy_filtered/runs/

# Cloned reference codebases (large, fetched separately)
codebases/

# Other research projects co-located in the same workspace are git-ignored
# from this repo's perspective — only files in this directory belong here.
